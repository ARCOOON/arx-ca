# Optional build-time override for the API base URL.
# When unset, the built app targets same-origin /api/v1 (the packaged WebUI
# reverse-proxies /api to the Go backend). Set this only when serving the SPA
# from a different origin than the API.
# VITE_API_BASE_URL=https://localhost:8443/api/v1
